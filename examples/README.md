Examples

Recommended flow:

`Extract Chapter References → Consolidate References → Generate H3 Prompts → Select H3 Scene → MiniMax H3 Reference to Video`

Attach image references to the MiniMax node in the exact order emitted by
`Select H3 Scene`. Attach voice/audio references in its emitted audio order.
The novel pipeline never emits video references.
